import 'package:flutter/material.dart';

/// Colors lifted directly from the original React app's inline styles.
class AppColors {
  static const bg = Color(0xFF030712);
  static const bgPanel = Color(0xFF0A0F1A);
  static const bgPanelAlt = Color(0xFF06090F);
  static const bgCard = Color(0xFF090E1C);
  static const border = Color(0xFF1E293B);
  static const borderDim = Color(0xFF0F1E30);
  static const textPrimary = Color(0xFFF1F5F9);
  static const textSecondary = Color(0xFF94A3B8);
  static const textMuted = Color(0xFF475569);
  static const textFaint = Color(0xFF1E293B);

  static const purple = Color(0xFF7C3AED); // Dean
  static const purpleLight = Color(0xFFC4B5FD);
  static const teal = Color(0xFF0D9488); // Faculty
  static const tealLight = Color(0xFF5EEAD4);
  static const blue = Color(0xFF2563EB); // Student
  static const blueLight = Color(0xFF93C5FD);

  static const green = Color(0xFF22C55E); // free / live
  static const red = Color(0xFFEF4444); // busy / error
  static const redLight = Color(0xFFF87171);
  static const amber = Color(0xFFFBBF24); // break/lunch
}
